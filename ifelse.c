#include <studio.h>
void main() {
    int age;
    printf("\n Enter your age");
    scanf("\n %d",&age);
    if( age > 18) {
        printf("\n You are eligible for vote")
    }else {
        printf("\n You are not eligible for vote")
    }
}